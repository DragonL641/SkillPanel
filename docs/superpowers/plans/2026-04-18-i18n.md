# SkillPanel UI 国际化 (i18n) 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 SkillPanel 前端 UI 添加中文/英文双语支持，使用 react-i18next。

**Architecture:** react-i18next + i18next-browser-languagedetector 管理语言状态和持久化。翻译文件以 JSON 格式存放在 `public/locales/` 下。所有前端组件中的硬编码字符串替换为 `t()` 调用。

**Tech Stack:** i18next, react-i18next, i18next-browser-languagedetector

**Design Spec:** `docs/superpowers/specs/2026-04-18-i18n-design.md`

---

### Task 1: 安装依赖 + 创建翻译文件 + i18n 初始化

**Files:**
- Create: `public/locales/zh-CN/translation.json`
- Create: `public/locales/en/translation.json`
- Create: `src/i18n.ts`
- Modify: `index.html`
- Modify: `src/main.tsx`
- Modify: `package.json` (via npm install)

- [ ] **Step 1: 安装 i18n 依赖**

```bash
npm install i18next react-i18next i18next-browser-languagedetector
```

- [ ] **Step 2: 创建中文翻译文件 `public/locales/zh-CN/translation.json`**

```json
{
  "app": {
    "searchPlaceholder": "搜索技能...",
    "searchLabel": "搜索技能",
    "loading": "加载中...",
    "refresh": "刷新",
    "settings": "设置",
    "closeError": "关闭错误提示",
    "addProjectFirst": "请先添加项目",
    "selectProject": "请选择一个项目"
  },
  "tab": {
    "global": "全局 Skill 管理",
    "project": "项目级 Skill 管理",
    "plugin": "插件技能管理"
  },
  "stats": {
    "customSkills": "自定义技能",
    "customCount": "共 {{count}} 个自定义技能",
    "enabled": "已启用",
    "enabledRate": "{{rate}}% 启用率",
    "pluginSkills": "插件技能",
    "fromPlugins": "来自已安装插件",
    "total": "总计",
    "totalDesc": "所有技能总数"
  },
  "dirTree": {
    "skillCount": "{{count}} 个技能",
    "enableAll": "全部启用",
    "disableAll": "全部禁用",
    "empty": "暂无自定义 skills"
  },
  "skill": {
    "enabled": "已启用",
    "disabled": "未启用",
    "enableAria": "启用技能",
    "disableAria": "禁用技能",
    "enableFailed": "启用失败",
    "disableFailed": "禁用失败",
    "analyze": "分析",
    "analyzing": "分析中...",
    "deleteAria": "删除技能",
    "confirmDelete": "确定删除「{{name}}」？",
    "deleteLabel": "删除",
    "cancel": "取消",
    "deleteFailed": "删除失败",
    "batchFailed": "{{count}} 个 skill 操作失败",
    "batchFailedFallback": "批量操作失败",
    "loadFailed": "加载失败"
  },
  "analysis": {
    "failed": "分析失败",
    "expand": "原理分析",
    "collapse": "收起分析",
    "loading": "正在分析，请稍候...",
    "timestamp": "分析时间: {{date}}",
    "reanalyze": "重新分析",
    "notAnalyzed": "未分析",
    "startAnalysis": "开始分析"
  },
  "plugin": {
    "noMatch": "没有匹配的插件",
    "noPlugins": "暂无已安装的插件",
    "checkFailed": "检查失败",
    "upToDate": "已是最新",
    "behind": "落后 {{count}} 个 commit",
    "checking": "检查中...",
    "checkUpdate": "检查更新"
  },
  "config": {
    "title": "配置",
    "close": "关闭",
    "saveFailed": "保存失败",
    "claudeDir": "Claude Code 目录",
    "claudeDirPlaceholder": "~/.claude",
    "claudeDirHint": "skills/ 和 plugins/ 目录将从此自动推导",
    "customDir": "自定义技能目录",
    "customDirHint": "支持添加多个目录",
    "selectDir": "选择目录",
    "addDir": "添加目录",
    "port": "端口",
    "apiDetected": "API 配置已检测到",
    "apiNotDetected": "API 配置未检测到",
    "model": "模型: {{model}}",
    "analysisUnavailable": "分析功能不可用",
    "apiSource": "从 {{dir}}/settings.json 自动读取",
    "saving": "保存中...",
    "save": "保存配置"
  },
  "dirPicker": {
    "loadError": "无法加载目录",
    "notSelected": "未选择",
    "browse": "浏览",
    "clear": "清除",
    "close": "关闭",
    "goUp": "返回上级",
    "loading": "加载中...",
    "empty": "此目录下没有子目录",
    "selectCurrent": "选择当前目录"
  },
  "project": {
    "selectDir": "选择项目目录",
    "listTitle": "项目列表",
    "addAria": "添加项目",
    "add": "添加",
    "dirLabel": "项目路径",
    "adding": "添加中...",
    "dirHint": "选择项目根目录",
    "removeAria": "删除项目 {{name}}",
    "skillCount": "{{project}} 项目级 + {{global}} 全局",
    "empty": "暂无项目",
    "loading": "加载中...",
    "loadFailed": "加载失败"
  },
  "projectSkill": {
    "activeSkills": "已生效技能",
    "skillCount": "{{count}} 个技能",
    "addSkill": "添加技能",
    "globalReadOnly": "全局技能（只读）",
    "global": "全局",
    "projectSkills": "项目级技能",
    "deleteAria": "删除项目技能 {{name}}",
    "confirmDelete": "确定删除「{{name}}」？",
    "deleteLabel": "删除",
    "cancel": "取消",
    "empty": "暂无已生效技能"
  },
  "addSkill": {
    "batchFailed": "{{count}} 个技能添加失败: {{paths}}",
    "failed": "添加失败，请重试",
    "title": "添加技能到「{{name}}」",
    "loading": "加载中...",
    "allAdded": "所有技能已添加",
    "selected": "已选 {{count}} 项",
    "selectPrompt": "请选择要添加的技能",
    "adding": "添加中...",
    "confirm": "确认添加"
  },
  "langSwitch": {
    "zh": "中",
    "en": "EN"
  }
}
```

- [ ] **Step 3: 创建英文翻译文件 `public/locales/en/translation.json`**

```json
{
  "app": {
    "searchPlaceholder": "Search skills...",
    "searchLabel": "Search skills",
    "loading": "Loading...",
    "refresh": "Refresh",
    "settings": "Settings",
    "closeError": "Dismiss error",
    "addProjectFirst": "Please add a project first",
    "selectProject": "Please select a project"
  },
  "tab": {
    "global": "Global Skills",
    "project": "Project Skills",
    "plugin": "Plugin Skills"
  },
  "stats": {
    "customSkills": "Custom Skills",
    "customCount": "{{count}} custom skills total",
    "enabled": "Enabled",
    "enabledRate": "{{rate}}% enabled",
    "pluginSkills": "Plugin Skills",
    "fromPlugins": "From installed plugins",
    "total": "Total",
    "totalDesc": "All skills combined"
  },
  "dirTree": {
    "skillCount": "{{count}} skills",
    "enableAll": "Enable All",
    "disableAll": "Disable All",
    "empty": "No custom skills yet"
  },
  "skill": {
    "enabled": "Enabled",
    "disabled": "Disabled",
    "enableAria": "Enable skill",
    "disableAria": "Disable skill",
    "enableFailed": "Failed to enable",
    "disableFailed": "Failed to disable",
    "analyze": "Analyze",
    "analyzing": "Analyzing...",
    "deleteAria": "Delete skill",
    "confirmDelete": "Delete \"{{name}}\"?",
    "deleteLabel": "Delete",
    "cancel": "Cancel",
    "deleteFailed": "Failed to delete",
    "batchFailed": "{{count}} skill(s) failed",
    "batchFailedFallback": "Batch operation failed",
    "loadFailed": "Failed to load"
  },
  "analysis": {
    "failed": "Analysis failed",
    "expand": "How it works",
    "collapse": "Collapse",
    "loading": "Analyzing, please wait...",
    "timestamp": "Analyzed: {{date}}",
    "reanalyze": "Re-analyze",
    "notAnalyzed": "Not analyzed",
    "startAnalysis": "Start analysis"
  },
  "plugin": {
    "noMatch": "No matching plugins",
    "noPlugins": "No plugins installed",
    "checkFailed": "Check failed",
    "upToDate": "Up to date",
    "behind": "{{count}} commit(s) behind",
    "checking": "Checking...",
    "checkUpdate": "Check for updates"
  },
  "config": {
    "title": "Settings",
    "close": "Close",
    "saveFailed": "Failed to save",
    "claudeDir": "Claude Code Directory",
    "claudeDirPlaceholder": "~/.claude",
    "claudeDirHint": "skills/ and plugins/ directories will be derived from this",
    "customDir": "Custom Skill Directories",
    "customDirHint": "Supports multiple directories",
    "selectDir": "Select directory",
    "addDir": "Add directory",
    "port": "Port",
    "apiDetected": "API configuration detected",
    "apiNotDetected": "API configuration not detected",
    "model": "Model: {{model}}",
    "analysisUnavailable": "Analysis unavailable",
    "apiSource": "Auto-loaded from {{dir}}/settings.json",
    "saving": "Saving...",
    "save": "Save Settings"
  },
  "dirPicker": {
    "loadError": "Failed to load directory",
    "notSelected": "Not selected",
    "browse": "Browse",
    "clear": "Clear",
    "close": "Close",
    "goUp": "Go up",
    "loading": "Loading...",
    "empty": "No subdirectories",
    "selectCurrent": "Select this directory"
  },
  "project": {
    "selectDir": "Select project directory",
    "listTitle": "Projects",
    "addAria": "Add project",
    "add": "Add",
    "dirLabel": "Project path",
    "adding": "Adding...",
    "dirHint": "Select project root directory",
    "removeAria": "Remove project {{name}}",
    "skillCount": "{{project}} project + {{global}} global",
    "empty": "No projects",
    "loading": "Loading...",
    "loadFailed": "Failed to load"
  },
  "projectSkill": {
    "activeSkills": "Active Skills",
    "skillCount": "{{count}} skills",
    "addSkill": "Add Skill",
    "globalReadOnly": "Global Skills (read-only)",
    "global": "Global",
    "projectSkills": "Project Skills",
    "deleteAria": "Delete project skill {{name}}",
    "confirmDelete": "Delete \"{{name}}\"?",
    "deleteLabel": "Delete",
    "cancel": "Cancel",
    "empty": "No active skills"
  },
  "addSkill": {
    "batchFailed": "{{count}} skill(s) failed to add: {{paths}}",
    "failed": "Failed to add, please retry",
    "title": "Add skills to \"{{name}}\"",
    "loading": "Loading...",
    "allAdded": "All skills already added",
    "selected": "{{count}} selected",
    "selectPrompt": "Select skills to add",
    "adding": "Adding...",
    "confirm": "Confirm"
  },
  "langSwitch": {
    "zh": "中",
    "en": "EN"
  }
}
```

- [ ] **Step 4: 创建 `src/i18n.ts`**

```ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';

import zhCN from '../../public/locales/zh-CN/translation.json';
import en from '../../public/locales/en/translation.json';

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      'zh-CN': { translation: zhCN },
      en: { translation: en },
    },
    fallbackLng: 'zh-CN',
    interpolation: { escapeValue: false },
    detection: {
      order: ['localStorage', 'navigator'],
      lookupLocalStorage: 'skillpanel-lang',
      caches: ['localStorage'],
    },
  });

document.documentElement.lang = i18n.language;
i18n.on('languageChanged', (lng) => {
  document.documentElement.lang = lng;
});

export default i18n;
```

- [ ] **Step 5: 修改 `index.html` — 移除硬编码 lang**

将 `<html lang="zh-CN">` 改为 `<html>`（i18n 初始化时动态设置）：

```html
<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SkillPanel</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 6: 修改 `src/main.tsx` — 在 React 渲染前导入 i18n**

在第一行添加 `import './i18n';`：

```tsx
import './i18n';
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

- [ ] **Step 7: 验证开发服务器正常启动**

```bash
npm run dev
```

Expected: 服务器正常启动，UI 仍显示中文（默认回退到 zh-CN），无控制台错误。

- [ ] **Step 8: Commit**

```bash
git add public/locales/ src/i18n.ts src/main.tsx index.html package.json package-lock.json
git commit -m "feat(i18n): add react-i18next foundation with zh-CN and en translations"
```

---

### Task 2: 创建语言切换组件 LangSwitch

**Files:**
- Create: `src/components/LangSwitch.tsx`
- Modify: `src/App.tsx` (仅 header 区域添加 LangSwitch)

- [ ] **Step 1: 创建 `src/components/LangSwitch.tsx`**

```tsx
import { useTranslation } from 'react-i18next';

export default function LangSwitch() {
  const { i18n, t } = useTranslation();

  const toggle = () => {
    const next = i18n.language === 'zh-CN' ? 'en' : 'zh-CN';
    i18n.changeLanguage(next);
  };

  return (
    <button
      onClick={toggle}
      className="px-2 py-1 text-[11px] font-medium text-fg-secondary border border-border rounded-[var(--radius-md)] hover:bg-surface-hover transition-colors"
    >
      {i18n.language === 'zh-CN' ? t('langSwitch.en') : t('langSwitch.zh')}
    </button>
  );
}
```

- [ ] **Step 2: 修改 `src/App.tsx` — 导入并添加 LangSwitch 到 header**

在 App.tsx 的 imports 中添加：
```tsx
import LangSwitch from './components/LangSwitch';
```

在 header 中，settings 按钮之前添加 LangSwitch：
```tsx
        {/* Language */}
        <LangSwitch />

        {/* Config */}
        <button
          onClick={() => setConfigOpen(true)}
          aria-label="设置"
          className="p-2 text-fg-secondary hover:text-fg-primary hover:bg-surface-hover rounded-[var(--radius-md)] transition-colors"
        >
          <Settings size={18} />
        </button>
```

- [ ] **Step 3: 验证语言切换功能**

打开浏览器，点击 header 中的语言切换按钮，确认：
- 点击后 UI 整体文字不变（因为其他组件还没有 t()，还是硬编码）
- LangSwitch 按钮本身文字在 `EN` 和 `中` 之间切换
- 刷新页面后保持选择的语言（检查 localStorage 中 `skillpanel-lang` 值）

- [ ] **Step 4: Commit**

```bash
git add src/components/LangSwitch.tsx src/App.tsx
git commit -m "feat(i18n): add LangSwitch component in header"
```

---

### Task 3: 迁移 TabSwitch + StatsRow + DirTree

**Files:**
- Modify: `src/components/TabSwitch.tsx`
- Modify: `src/components/StatsRow.tsx`
- Modify: `src/components/DirTree.tsx`

- [ ] **Step 1: 迁移 `src/components/TabSwitch.tsx`**

替换为：
```tsx
import { useTranslation } from 'react-i18next';
import { FolderCode, FolderTree, Puzzle } from 'lucide-react';

export type TabKey = 'global' | 'project' | 'plugin';

interface Props {
  active: TabKey;
  onChange: (tab: TabKey) => void;
}

const tabKeys: { key: TabKey; labelKey: string; icon: typeof FolderCode }[] = [
  { key: 'global', labelKey: 'tab.global', icon: FolderCode },
  { key: 'project', labelKey: 'tab.project', icon: FolderTree },
  { key: 'plugin', labelKey: 'tab.plugin', icon: Puzzle },
];

export default function TabSwitch({ active, onChange }: Props) {
  const { t } = useTranslation();

  return (
    <div className="flex gap-1 p-1 bg-surface-tertiary rounded-[var(--radius-md)]">
      {tabKeys.map((tab) => {
        const Icon = tab.icon;
        const isActive = active === tab.key;
        return (
          <button
            key={tab.key}
            onClick={() => onChange(tab.key)}
            className={`flex items-center gap-1.5 px-3.5 py-1.5 text-xs font-medium rounded-[var(--radius-sm)] transition-all ${
              isActive
                ? 'bg-surface-primary text-fg-primary shadow-[0_1px_2px_rgba(0,0,0,0.04)]'
                : 'text-fg-secondary hover:text-fg-primary'
            }`}
          >
            <Icon size={14} />
            {t(tab.labelKey)}
          </button>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 2: 迁移 `src/components/StatsRow.tsx`**

替换为：
```tsx
import { useTranslation } from 'react-i18next';
import type { Summary } from '../types';

interface Props {
  data: Summary | null;
}

export default function StatsRow({ data }: Props) {
  const { t } = useTranslation();

  const stats = data
    ? [
        { label: t('stats.customSkills'), value: String(data.customTotal), sub: t('stats.customCount', { count: data.customTotal }) },
        { label: t('stats.enabled'), value: String(data.customEnabled), sub: t('stats.enabledRate', { rate: data.customTotal ? Math.round((data.customEnabled / data.customTotal) * 100) : 0 }), accent: 'text-success' },
        { label: t('stats.pluginSkills'), value: String(data.pluginTotal), sub: t('stats.fromPlugins') },
        { label: t('stats.total'), value: String(data.grandTotal), sub: t('stats.totalDesc') },
      ]
    : [
        { label: t('stats.customSkills'), value: '-', sub: '' },
        { label: t('stats.enabled'), value: '-', sub: '' },
        { label: t('stats.pluginSkills'), value: '-', sub: '' },
        { label: t('stats.total'), value: '-', sub: '' },
      ];

  return (
    <div className="flex flex-wrap gap-4">
      {stats.map((stat) => (
        <div
          key={stat.label}
          className="flex-1 min-w-[140px] flex flex-col gap-2 p-5 bg-surface-primary rounded-[var(--radius-lg)] border border-border"
        >
          <span className="text-xs font-medium text-fg-secondary">{stat.label}</span>
          <span className={`font-mono text-3xl font-bold text-fg-primary ${stat.accent ?? ''}`}>
            {stat.value}
          </span>
          {stat.sub && <span className="text-[11px] text-fg-muted">{stat.sub}</span>}
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 3: 迁移 `src/components/DirTree.tsx`**

在文件顶部添加 import：
```tsx
import { useTranslation } from 'react-i18next';
```

在 `TreeNodeItem` 函数体内第一行添加：
```tsx
const { t } = useTranslation();
```

替换第 89 行的 `childCount` 文本：
```tsx
// 原: {childCount} 个技能
{t('dirTree.skillCount', { count: childCount })}
```

替换第 96 行的 `全部启用`：
```tsx
// 原: 全部启用
{t('dirTree.enableAll')}
```

替换第 103 行的 `全部禁用`：
```tsx
// 原: 全部禁用
{t('dirTree.disableAll')}
```

替换第 142 行空状态：
```tsx
// 原: 暂无自定义 skills
{t('dirTree.empty')}
```

- [ ] **Step 4: 验证这三个组件的语言切换**

在浏览器中切换语言，确认 TabSwitch、StatsRow、DirTree 的文本正确切换。

- [ ] **Step 5: Commit**

```bash
git add src/components/TabSwitch.tsx src/components/StatsRow.tsx src/components/DirTree.tsx
git commit -m "feat(i18n): migrate TabSwitch, StatsRow, and DirTree to use translations"
```

---

### Task 4: 迁移 SkillCard + AnalysisPanel

**Files:**
- Modify: `src/components/SkillCard.tsx`
- Modify: `src/components/AnalysisPanel.tsx`

- [ ] **Step 1: 迁移 `src/components/SkillCard.tsx`**

在文件顶部添加 import：
```tsx
import { useTranslation } from 'react-i18next';
```

在 `SkillCard` 函数体内第一行添加：
```tsx
const { t } = useTranslation();
```

逐项替换硬编码字符串：

第 32 行 `已启用`：
```tsx
{t('skill.enabled')}
```

第 36 行 `未启用`：
```tsx
{t('skill.disabled')}
```

第 57 行 aria-label：
```tsx
aria-label={skill.enabled ? t('skill.disableAria') : t('skill.enableAria')}
```

第 69 行按钮文本：
```tsx
{analyzing ? t('skill.analyzing') : t('skill.analyze')}
```

第 75 行 aria-label：
```tsx
aria-label={t('skill.deleteAria')}
```

第 86 行确认文本：
```tsx
<span className="text-danger">{t('skill.confirmDelete', { name: skill.name })}</span>
```

第 90 行删除按钮：
```tsx
{t('skill.deleteLabel')}
```

第 95 行取消按钮：
```tsx
{t('skill.cancel')}
```

- [ ] **Step 2: 迁移 `src/components/AnalysisPanel.tsx`**

在文件顶部添加 import：
```tsx
import { useTranslation } from 'react-i18next';
```

在 `AnalysisPanel` 的 forwardRef 内部函数体第一行添加：
```tsx
const { t, i18n } = useTranslation();
```

逐项替换：

第 40 行错误 fallback：
```tsx
// 原: setError(getErrorMessage(err) || '分析失败');
setError(getErrorMessage(err) || t('analysis.failed'));
```

第 82 行展开按钮：
```tsx
{t('analysis.expand')}
```

第 94 行收起按钮：
```tsx
{t('analysis.collapse')}
```

第 99 行加载文本：
```tsx
{t('analysis.loading')}
```

第 110 行时间戳（注意同时修复日期 locale）：
```tsx
// 原: 分析时间: {new Date(analyzedAt).toLocaleString()}
{t('analysis.timestamp', { date: new Date(analyzedAt).toLocaleString(i18n.language) })}
```

第 117 行重新分析按钮：
```tsx
{t('analysis.reanalyze')}
```

第 128 行未分析标签：
```tsx
{t('analysis.notAnalyzed')}
```

第 133 行开始分析按钮：
```tsx
{t('analysis.startAnalysis')}
```

- [ ] **Step 3: 验证 SkillCard 和 AnalysisPanel 语言切换**

- [ ] **Step 4: Commit**

```bash
git add src/components/SkillCard.tsx src/components/AnalysisPanel.tsx
git commit -m "feat(i18n): migrate SkillCard and AnalysisPanel to use translations"
```

---

### Task 5: 迁移 PluginPanel

**Files:**
- Modify: `src/components/PluginPanel.tsx`

- [ ] **Step 1: 迁移 `src/components/PluginPanel.tsx`**

在文件顶部添加 import：
```tsx
import { useTranslation } from 'react-i18next';
```

修改 `formatDate` 函数，接受 locale 参数：
```tsx
function formatDate(iso: string, locale: string): string {
  try {
    return new Date(iso).toLocaleDateString(locale);
  } catch {
    return iso;
  }
}
```

在 `PluginPanel` 函数体内添加：
```tsx
const { t, i18n } = useTranslation();
```

替换第 52 行空状态：
```tsx
{normalizedFilter ? t('plugin.noMatch') : t('plugin.noPlugins')}
```

在 `PluginCard` 函数体内添加：
```tsx
const { t, i18n } = useTranslation();
```

替换第 85 行错误 fallback：
```tsx
// 原: setUpdateStatus({ error: getErrorMessage(err) || '检查失败' });
setUpdateStatus({ error: getErrorMessage(err) || t('plugin.checkFailed') });
```

替换第 92 行 dateLabel 使用 locale：
```tsx
// 原: const dateLabel = plugin.lastUpdated ? formatDate(plugin.lastUpdated) : '';
const dateLabel = plugin.lastUpdated ? formatDate(plugin.lastUpdated, i18n.language) : '';
```

替换第 116 行已是最新：
```tsx
{t('plugin.upToDate')}
```

替换第 119 行落后提示：
```tsx
{t('plugin.behind', { count: updateStatus.behindBy })}
```

替换第 130 行检查更新按钮：
```tsx
{checking ? t('plugin.checking') : t('plugin.checkUpdate')}
```

在 `PluginSkillRow` 函数体内添加：
```tsx
const { t } = useTranslation();
```

替换第 163 行分析按钮：
```tsx
{analyzing ? t('skill.analyzing') : t('skill.analyze')}
```

- [ ] **Step 2: 验证 PluginPanel 语言切换**

- [ ] **Step 3: Commit**

```bash
git add src/components/PluginPanel.tsx
git commit -m "feat(i18n): migrate PluginPanel to use translations with locale-aware dates"
```

---

### Task 6: 迁移 ConfigModal + DirPicker

**Files:**
- Modify: `src/components/ConfigModal.tsx`
- Modify: `src/components/DirPicker.tsx`

- [ ] **Step 1: 迁移 `src/components/ConfigModal.tsx`**

在文件顶部添加 import：
```tsx
import { useTranslation } from 'react-i18next';
```

在 `ConfigModal` 函数体内添加：
```tsx
const { t } = useTranslation();
```

逐项替换硬编码字符串：

第 121 行保存失败：
```tsx
// 原: setSaveError(getErrorMessage(err) || '保存失败');
setSaveError(getErrorMessage(err) || t('config.saveFailed'));
```

第 141 行配置标题：
```tsx
{t('config.title')}
```

第 143 行关闭按钮 aria-label：
```tsx
aria-label={t('config.close')}
```

第 151 行 Claude 目录标签：
```tsx
{t('config.claudeDir')}
```

第 156 行 placeholder：
```tsx
placeholder={t('config.claudeDirPlaceholder')}
```

第 159 行提示文本：
```tsx
{t('config.claudeDirHint')}
```

第 163 行自定义目录标签：
```tsx
{t('config.customDir')}
```

第 164 行提示文本：
```tsx
{t('config.customDirHint')}
```

第 179 行 DirPicker label：
```tsx
label={t('config.selectDir')}
```

第 184 行添加目录按钮：
```tsx
{t('config.addDir')}
```

第 191 行端口标签：
```tsx
{t('config.port')}
```

第 209 行 API 状态：
```tsx
{apiConfigDetected ? t('config.apiDetected') : t('config.apiNotDetected')}
```

第 213 行模型：
```tsx
{t('config.model', { model: apiModel })}
```

第 216 行分析不可用：
```tsx
{t('config.analysisUnavailable')}
```

第 219 行 API 来源提示：
```tsx
{t('config.apiSource', { dir: config.claudeRootDir || '~/.claude' })}
```

第 234 行取消按钮：
```tsx
{t('skill.cancel')}
```

第 241 行保存按钮：
```tsx
{saving ? t('config.saving') : t('config.save')}
```

- [ ] **Step 2: 迁移 `src/components/DirPicker.tsx`**

在文件顶部添加 import：
```tsx
import { useTranslation } from 'react-i18next';
```

在 `DirPicker` 函数体内添加：
```tsx
const { t } = useTranslation();
```

逐项替换：

第 27 行错误 fallback：
```tsx
// 原: setError(getErrorMessage(err) || '无法加载目录');
setError(getErrorMessage(err) || t('dirPicker.loadError'));
```

第 71 行未选择：
```tsx
// 原: {value || '未选择'}
{value || t('dirPicker.notSelected')}
```

第 77 行浏览按钮：
```tsx
{t('dirPicker.browse')}
```

第 84 行清除按钮：
```tsx
{t('dirPicker.clear')}
```

第 98 行关闭按钮：
```tsx
{t('dirPicker.close')}
```

第 117 行返回上级：
```tsx
{t('dirPicker.goUp')}
```

第 125 行加载中：
```tsx
{t('dirPicker.loading')}
```

第 131 行空目录：
```tsx
{t('dirPicker.empty')}
```

第 153 行选择当前目录：
```tsx
{t('dirPicker.selectCurrent')}
```

- [ ] **Step 3: 验证 ConfigModal 和 DirPicker 语言切换**

打开设置弹窗，切换语言，确认所有文本正确切换。

- [ ] **Step 4: Commit**

```bash
git add src/components/ConfigModal.tsx src/components/DirPicker.tsx
git commit -m "feat(i18n): migrate ConfigModal and DirPicker to use translations"
```

---

### Task 7: 迁移 ProjectSidebar + ProjectSkillView + AddSkillModal

**Files:**
- Modify: `src/components/ProjectSidebar.tsx`
- Modify: `src/components/ProjectSkillView.tsx`
- Modify: `src/components/AddSkillModal.tsx`

- [ ] **Step 1: 迁移 `src/components/ProjectSidebar.tsx`**

在文件顶部添加 import：
```tsx
import { useTranslation } from 'react-i18next';
```

在 `ProjectSidebar` 函数体内添加：
```tsx
const { t } = useTranslation();
```

逐项替换：

第 22 行 pickFolder 标题：
```tsx
// 原: const result = await pickFolder('选择项目目录');
const result = await pickFolder(t('project.selectDir'));
```

第 53 行项目列表标题：
```tsx
{t('project.listTitle')}
```

第 58 行添加按钮 aria-label：
```tsx
aria-label={t('project.addAria')}
```

第 61 行添加按钮文本：
```tsx
{t('project.add')}
```

第 71 行 DirPicker label：
```tsx
label={t('project.dirLabel')}
```

第 72 行 DirPicker hint：
```tsx
hint={adding ? t('project.adding') : t('project.dirHint')}
```

第 102 行删除按钮 aria-label：
```tsx
aria-label={t('project.removeAria', { name: project.name })}
```

第 111 行技能计数：
```tsx
{t('project.skillCount', { project: project.projectEnabledCount, global: project.globalEnabledCount })}
```

第 120 行空状态：
```tsx
{t('project.empty')}
```

第 125 行加载中：
```tsx
{t('project.loading')}
```

- [ ] **Step 2: 迁移 `src/components/ProjectSkillView.tsx`**

在文件顶部添加 import：
```tsx
import { useTranslation } from 'react-i18next';
```

在 `ProjectSkillView` 函数体内添加：
```tsx
const { t } = useTranslation();
```

逐项替换：

第 22 行已生效技能标题：
```tsx
{t('projectSkill.activeSkills')}
```

第 24 行技能计数：
```tsx
{t('projectSkill.skillCount', { count: (skills.globalSkills?.length || 0) + (skills.projectSkills?.length || 0) })}
```

第 30 行添加技能按钮：
```tsx
{t('projectSkill.addSkill')}
```

第 40 行全局技能标签：
```tsx
{t('projectSkill.globalReadOnly')}
```

第 54 行全局 badge：
```tsx
{t('projectSkill.global')}
```

第 71 行项目级技能标签：
```tsx
{t('projectSkill.projectSkills')}
```

第 87 行删除按钮 aria-label：
```tsx
aria-label={t('projectSkill.deleteAria', { name: skill.name })}
```

第 98 行确认删除：
```tsx
<span className="text-danger">{t('projectSkill.confirmDelete', { name: skill.name })}</span>
```

第 101 行删除按钮：
```tsx
{t('projectSkill.deleteLabel')}
```

第 107 行取消按钮：
```tsx
{t('projectSkill.cancel')}
```

第 123 行空状态：
```tsx
{t('projectSkill.empty')}
```

- [ ] **Step 3: 迁移 `src/components/AddSkillModal.tsx`**

在文件顶部添加 import：
```tsx
import { useTranslation } from 'react-i18next';
```

在 `AddSkillModal` 函数体内添加：
```tsx
const { t } = useTranslation();
```

逐项替换：

第 68 行批量失败错误：
```tsx
// 原: setError(`${result.failed.length} 个技能添加失败: ${result.failed.map(f => f.path).join(', ')}`);
setError(t('addSkill.batchFailed', { count: result.failed.length, paths: result.failed.map(f => f.path).join(', ') }));
```

第 74 行添加失败：
```tsx
// 原: setError('添加失败，请重试');
setError(t('addSkill.failed'));
```

第 88 行标题：
```tsx
{t('addSkill.title', { name: projectName })}
```

第 105 行加载中：
```tsx
{t('addSkill.loading')}
```

第 107 行所有技能已添加：
```tsx
{t('addSkill.allAdded')}
```

第 135 行选择计数：
```tsx
{selected.size > 0 ? t('addSkill.selected', { count: selected.size }) : t('addSkill.selectPrompt')}
```

第 142 行确认按钮：
```tsx
{submitting ? t('addSkill.adding') : t('addSkill.confirm')}
```

- [ ] **Step 4: 验证项目相关组件语言切换**

切换到项目 tab，切换语言，确认所有文本正确切换。

- [ ] **Step 5: Commit**

```bash
git add src/components/ProjectSidebar.tsx src/components/ProjectSkillView.tsx src/components/AddSkillModal.tsx
git commit -m "feat(i18n): migrate ProjectSidebar, ProjectSkillView, and AddSkillModal"
```

---

### Task 8: 迁移 App.tsx + hooks

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/hooks/useSkills.ts`
- Modify: `src/hooks/usePlugins.ts`

- [ ] **Step 1: 迁移 `src/App.tsx` 中剩余的硬编码字符串**

在 App.tsx 的 imports 中添加：
```tsx
import { useTranslation } from 'react-i18next';
```

在 `App` 函数体内添加：
```tsx
const { t } = useTranslation();
```

替换搜索框 placeholder：
```tsx
// 原: placeholder="搜索技能..."
placeholder={t('app.searchPlaceholder')}
```

替换搜索框 aria-label：
```tsx
// 原: aria-label="搜索技能"
aria-label={t('app.searchLabel')}
```

替换刷新按钮 aria-label：
```tsx
// 原: aria-label="刷新"
aria-label={t('app.refresh')}
```

替换设置按钮 aria-label：
```tsx
// 原: aria-label="设置"
aria-label={t('app.settings')}
```

替换关闭错误按钮 aria-label：
```tsx
// 原: aria-label="关闭错误提示"
aria-label={t('app.closeError')}
```

替换加载文本：
```tsx
// 原: 加载中...
{t('app.loading')}
```

替换空状态文本：
```tsx
// 原: {projects.length === 0 ? '请先添加项目' : '请选择一个项目'}
{projects.length === 0 ? t('app.addProjectFirst') : t('app.selectProject')}
```

- [ ] **Step 2: 迁移 `src/hooks/useSkills.ts`**

在文件顶部添加 import：
```tsx
import { useTranslation } from 'react-i18next';
```

在 `useSkills` 函数体内第一行添加：
```tsx
const { t } = useTranslation();
```

逐项替换 fallback 字符串：

第 29 行加载失败：
```tsx
// 原: setError(getErrorMessage(err) || '加载失败');
setError(getErrorMessage(err) || t('skill.loadFailed'));
```

第 43 行启用/禁用失败：
```tsx
// 原: setError(getErrorMessage(err) || (enable ? '启用失败' : '禁用失败'));
setError(getErrorMessage(err) || (enable ? t('skill.enableFailed') : t('skill.disableFailed')));
```

第 52 行批量失败：
```tsx
// 原: setError(`${result.failed.length} 个 skill 操作失败`);
setError(t('skill.batchFailed', { count: result.failed.length }));
```

第 57 行批量失败 fallback：
```tsx
// 原: setError(getErrorMessage(err) || '批量操作失败');
setError(getErrorMessage(err) || t('skill.batchFailedFallback'));
```

第 68 行删除失败：
```tsx
// 原: setError(getErrorMessage(err) || '删除失败');
setError(getErrorMessage(err) || t('skill.deleteFailed'));
```

- [ ] **Step 3: 迁移 `src/hooks/usePlugins.ts`**

在文件顶部添加 import：
```tsx
import { useTranslation } from 'react-i18next';
```

在 `usePlugins` 函数体内第一行添加：
```tsx
const { t } = useTranslation();
```

替换第 18 行加载失败：
```tsx
// 原: setError(getErrorMessage(err) || '加载失败');
setError(getErrorMessage(err) || t('skill.loadFailed'));
```

- [ ] **Step 4: 验证所有组件语言切换完成**

在浏览器中全面测试：
- 切换到英文，遍历所有 tab 和弹窗，确认无遗漏的中文字符串
- 切换回中文，确认无遗漏的英文字符串
- 刷新页面确认语言持久化

- [ ] **Step 5: Commit**

```bash
git add src/App.tsx src/hooks/useSkills.ts src/hooks/usePlugins.ts
git commit -m "feat(i18n): complete migration of App.tsx and hooks to use translations"
```

---

### Task 9: 翻译 Key 一致性测试

**Files:**
- Create: `src/__tests__/i18n-consistency.test.ts`

- [ ] **Step 1: 编写 key 一致性测试 `src/__tests__/i18n-consistency.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import zhCN from '../../public/locales/zh-CN/translation.json';
import en from '../../public/locales/en/translation.json';

function collectKeys(obj: Record<string, unknown>, prefix = ''): string[] {
  const keys: string[] = [];
  for (const [key, value] of Object.entries(obj)) {
    const fullKey = prefix ? `${prefix}.${key}` : key;
    if (value && typeof value === 'object' && !Array.isArray(value)) {
      keys.push(...collectKeys(value as Record<string, unknown>, fullKey));
    } else {
      keys.push(fullKey);
    }
  }
  return keys;
}

describe('i18n translation key consistency', () => {
  const zhKeys = new Set(collectKeys(zhCN));
  const enKeys = new Set(collectKeys(en));

  it('should have the same keys in zh-CN and en', () => {
    const missingInEn = [...zhKeys].filter((k) => !enKeys.has(k));
    const missingInZh = [...enKeys].filter((k) => !zhKeys.has(k));

    expect(missingInEn, `Keys missing in en: ${missingInEn.join(', ')}`).toEqual([]);
    expect(missingInZh, `Keys missing in zh-CN: ${missingInZh.join(', ')}`).toEqual([]);
  });

  it('should not have empty values in en', () => {
    const enFlat = collectKeys(en);
    const emptyKeys = enFlat.filter((key) => {
      const parts = key.split('.');
      let obj: Record<string, unknown> = en;
      for (const part of parts) {
        obj = obj[part] as Record<string, unknown>;
      }
      return !obj || (typeof obj === 'string' && obj.trim() === '');
    });
    expect(emptyKeys, `Empty values in en: ${emptyKeys.join(', ')}`).toEqual([]);
  });
});
```

- [ ] **Step 2: 运行测试**

```bash
npx vitest --run src/__tests__/i18n-consistency.test.ts
```

Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add src/__tests__/i18n-consistency.test.ts
git commit -m "test(i18n): add translation key consistency test"
```

---

### Task 10: 最终构建验证

- [ ] **Step 1: 运行所有测试**

```bash
npx vitest --run
```

Expected: 所有测试 PASS

- [ ] **Step 2: 运行生产构建**

```bash
npm run build
```

Expected: 构建成功，无 TypeScript 错误

- [ ] **Step 3: 全面 QA 检查**

在浏览器中逐项验证：
- [ ] 切换语言后所有可见文本都正确切换
- [ ] 带插值的字符串在两种语言下都正确渲染
- [ ] localStorage 持久化生效，刷新页面保持选择
- [ ] 浏览器语言为英文时默认显示英文
- [ ] `<html>` 的 `lang` 属性随语言切换更新
- [ ] aria-label 在两种语言下都正确

- [ ] **Step 4: 最终 Commit（如有修复）**

```bash
git add -A
git commit -m "fix(i18n): final cleanup and fixes from QA"
```
